# ═══════════════════════════════════════════════════════════════════════════════
# 🌀 LIFE FRACTAL INTELLIGENCE v11.0 - MANUAL POWERSHELL COMMANDS
# ═══════════════════════════════════════════════════════════════════════════════
# Copy and paste these commands one at a time into PowerShell
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# STEP 1: Navigate to your project folder
# ─────────────────────────────────────────────────────────────────────────────
# CHANGE THIS to your actual project path:

cd C:\Users\YourUsername\Projects\planner

# OR if your project is elsewhere, use that path instead


# ─────────────────────────────────────────────────────────────────────────────
# STEP 2: Copy downloaded files from Downloads to project folder
# ─────────────────────────────────────────────────────────────────────────────
# Make sure you've downloaded app.py and requirements.txt from Claude first!

Copy-Item "$env:USERPROFILE\Downloads\app.py" -Destination ".\app.py" -Force
Copy-Item "$env:USERPROFILE\Downloads\requirements.txt" -Destination ".\requirements.txt" -Force


# ─────────────────────────────────────────────────────────────────────────────
# STEP 3: Verify the files were copied
# ─────────────────────────────────────────────────────────────────────────────

Get-ChildItem app.py, requirements.txt


# ─────────────────────────────────────────────────────────────────────────────
# STEP 4: Stage all files for git
# ─────────────────────────────────────────────────────────────────────────────

git add -A


# ─────────────────────────────────────────────────────────────────────────────
# STEP 5: Check what will be committed
# ─────────────────────────────────────────────────────────────────────────────

git status


# ─────────────────────────────────────────────────────────────────────────────
# STEP 6: Commit with message
# ─────────────────────────────────────────────────────────────────────────────

git commit -m "🌀 Life Fractal Intelligence v11.0 - Complete AI System with Pattern Recognition, Executive Dysfunction Detection, Multi-Layer Fractals, 3D Universe"


# ─────────────────────────────────────────────────────────────────────────────
# STEP 7: Push to GitHub (Render will auto-deploy)
# ─────────────────────────────────────────────────────────────────────────────

git push origin main --force


# ─────────────────────────────────────────────────────────────────────────────
# DONE! Check Render dashboard for deployment status
# ─────────────────────────────────────────────────────────────────────────────
# Your app: https://planner-1-pyd9.onrender.com
# Render Dashboard: https://dashboard.render.com


# ═══════════════════════════════════════════════════════════════════════════════
# TROUBLESHOOTING
# ═══════════════════════════════════════════════════════════════════════════════

# If git push fails with authentication error:
git push origin main

# If you need to set up credentials:
git config --global user.email "onlinediscountsllc@gmail.com"
git config --global user.name "Your Name"

# If branch is behind remote:
git pull origin main --rebase
git push origin main

# If you want to completely replace remote:
git push origin main --force

# To check remote URL:
git remote -v

# To see recent commits:
git log --oneline -5
