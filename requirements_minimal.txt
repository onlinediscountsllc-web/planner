# Life Planner - Minimal Requirements
# These are the core dependencies that will definitely install

# Core Framework (Required)
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==4.0.0
Werkzeug==3.0.1

# Database (Required)
SQLAlchemy==2.0.23

# Security (Required)
python-dotenv==1.0.0

# Optional but Recommended
Flask-JWT-Extended==4.5.3
Flask-Mail==0.9.1
bcrypt==4.1.2
email-validator==2.1.0

# Payment Processing (Optional)
stripe==7.8.0

# Math and ML (Optional - graceful degradation if missing)
numpy==1.26.2
Pillow==10.1.0
scikit-learn==1.3.2

# Utilities (Optional)
requests==2.31.0

# NOTE: If any package fails to install, the app will still work
# with reduced functionality. Run: pip install -r requirements.txt
