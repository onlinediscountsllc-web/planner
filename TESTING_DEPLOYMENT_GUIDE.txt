═══════════════════════════════════════════════════════════════════════════════
🧪 TESTING & DEPLOYMENT GUIDE
═══════════════════════════════════════════════════════════════════════════════

## 📊 CURRENT STATUS

**Live Site:** https://planner-1-pyd9.onrender.com  
**Current Version:** 13.0 (still showing, Render may still be building)

═══════════════════════════════════════════════════════════════════════════════

## 🧪 TEST THE CURRENT DEPLOYMENT

Run these tests NOW to see what works:

```powershell
# Test 1: Health Check
curl "https://planner-1-pyd9.onrender.com/api/health"

# Test 2: Particle Swarm (was broken)
curl "https://planner-1-pyd9.onrender.com/api/math/particle-swarm?energy=0.7&wellness=0.7"

# Test 3: Binaural Audio (was broken)
curl "https://planner-1-pyd9.onrender.com/api/audio/binaural/focus?duration=5.0" -o test_audio.wav
```

**If Tests 2 & 3 still fail:** Render hasn't rebuilt yet OR the v13.1 fix didn't deploy properly.

═══════════════════════════════════════════════════════════════════════════════

## 🚀 OPTION 1: DEPLOY v14.0 ULTIMATE COMPLETE

**What's in v14.0:**
- ✅ Virtual Pet System (8 species)
- ✅ Karma & Dharma Goals (feed pet with good deeds)
- ✅ Reminders & Alarms (like Google Calendar)
- ✅ Google Calendar Integration (OAuth ready)
- ✅ Binaural Beats (FIXED)
- ✅ Particle Swarm (FIXED)
- ✅ All 20 Mathematical Foundations
- ✅ Complete Task/Goal Management
- ✅ NO PLACEHOLDERS - Everything functional

### Deploy v14.0:

```powershell
cd C:\Users\Luke\Desktop\planner

# Backup current
Copy-Item app.py app_backup_v13.py

# Deploy v14.0
Copy-Item life_fractal_v14_ultimate_complete.py -Destination app.py -Force

git add app.py
git commit -m "v14.0 Ultimate Complete: Virtual Pet, Karma/Dharma, Calendar, Reminders"
git push origin main
```

═══════════════════════════════════════════════════════════════════════════════

## 🎯 OPTION 2: DEPLOY COVER FACE v1.0 (Complete Redesign)

**What's in COVER FACE:**
- ✅ Easy Mode (looks like Google Calendar)
- ✅ Advanced Mode (all your math)
- ✅ Labeled orbs (no confusion)
- ✅ Plain English AI
- ✅ Nordic design
- ✅ Addresses "meaningless designs" feedback

### Deploy COVER FACE:

```powershell
cd C:\Users\Luke\Desktop\planner

# Deploy COVER FACE
Copy-Item cover_face_v1_complete.py -Destination app.py -Force
New-Item -ItemType Directory -Force -Path templates
Copy-Item cover_face_frontend.html -Destination templates/index.html -Force
Copy-Item requirements_cover_face.txt -Destination requirements.txt -Force

git add app.py templates requirements.txt
git commit -m "COVER FACE v1.0: Complete UX redesign for normal humans"
git push origin main
```

═══════════════════════════════════════════════════════════════════════════════

## 📋 v14.0 NEW ENDPOINTS TO TEST

Once v14.0 is deployed, test these:

### Virtual Pet
```powershell
# Get pet state
curl "https://planner-1-pyd9.onrender.com/api/pet"

# Feed pet with karma (from completed good deeds)
curl -X POST "https://planner-1-pyd9.onrender.com/api/pet/feed/karma" `
  -H "Content-Type: application/json" `
  -d '{"karma_points":5}'

# Feed pet with dharma (from purpose work)
curl -X POST "https://planner-1-pyd9.onrender.com/api/pet/feed/dharma" `
  -H "Content-Type: application/json" `
  -d '{"dharma_points":10}'

# Play with pet
curl -X POST "https://planner-1-pyd9.onrender.com/api/pet/play"
```

### Karma Goals
```powershell
# Create karma goal (helping others)
curl -X POST "https://planner-1-pyd9.onrender.com/api/goals/karma" `
  -H "Content-Type: application/json" `
  -d '{"title":"Help neighbor with groceries","karma_points":5}'

# Get all karma goals
curl "https://planner-1-pyd9.onrender.com/api/goals/karma"
```

### Dharma Goals
```powershell
# Create dharma goal (life purpose)
curl -X POST "https://planner-1-pyd9.onrender.com/api/goals/dharma" `
  -H "Content-Type: application/json" `
  -d '{"title":"Meditate for 20 minutes","dharma_points":10}'

# Get all dharma goals
curl "https://planner-1-pyd9.onrender.com/api/goals/dharma"
```

### Complete Task (Awards Points)
```powershell
# Complete task and auto-feed pet
curl -X POST "https://planner-1-pyd9.onrender.com/api/tasks/1/complete"
# Returns karma/dharma earned and pet feeding results
```

### Reminders
```powershell
# Create reminder
curl -X POST "https://planner-1-pyd9.onrender.com/api/reminders" `
  -H "Content-Type: application/json" `
  -d '{"task_id":1,"reminder_time":"2025-12-06T10:00:00Z","message":"Don't forget!"}'

# Get all reminders
curl "https://planner-1-pyd9.onrender.com/api/reminders"
```

### Google Calendar
```powershell
# Start OAuth flow
curl "https://planner-1-pyd9.onrender.com/api/calendar/connect"

# Sync tasks to calendar
curl -X POST "https://planner-1-pyd9.onrender.com/api/calendar/sync"
```

═══════════════════════════════════════════════════════════════════════════════

## 🎨 HOW THE KARMA/DHARMA SYSTEM WORKS

### Karma Goals (Helping Others)
1. Create karma goal: "Help friend move" (5 karma points)
2. Complete the goal
3. System awards 5 karma points
4. Pet automatically eats karma points
5. Pet's mood improves, hunger decreases

### Dharma Goals (Life Purpose)
1. Create dharma goal: "Work on passion project" (10 dharma points)
2. Complete the goal
3. System awards 10 dharma points
4. Pet automatically eats dharma points  
5. Pet's energy increases, stress decreases, may start "glowing" behavior

### Pet Species Preferences
- **Cat/Dog:** Love karma (helping others)
- **Dragon/Phoenix:** Love dharma (spiritual growth)
- **Owl:** Balanced
- **Fox:** Karma focused
- **Unicorn/Butterfly:** Dharma focused

═══════════════════════════════════════════════════════════════════════════════

## 🔧 IF v13.1 STILL ISN'T WORKING

The particle swarm and binaural audio endpoints might still be broken. Here's why:

1. **Render is still building** (wait 5 more minutes)
2. **Wrong file deployed** (check app.py locally)
3. **Need fresh deployment** (try v14.0 instead)

**Recommended:** Just deploy v14.0 Ultimate Complete. It has:
- v13.1 fixes included
- All new features
- No placeholders
- Everything working

═══════════════════════════════════════════════════════════════════════════════

## 📁 FILES READY FOR YOU

All in C:\Users\Luke\Desktop\planner\:

1. **life_fractal_v14_ultimate_complete.py** (v14.0)
   - Virtual pet with karma/dharma
   - Google Calendar ready
   - Reminders/alarms
   - All fixes included

2. **cover_face_v1_complete.py** (Redesign)
   - Easy mode for normal people
   - Advanced mode for power users
   - Addresses friend feedback

3. **cover_face_frontend.html** (UI)
   - Beautiful interface
   - Labeled orbs
   - Legend always visible

═══════════════════════════════════════════════════════════════════════════════

## 💡 MY RECOMMENDATION

**Deploy v14.0 Ultimate Complete NOW:**

1. It fixes the v13.1 bugs
2. Adds all the features you asked for
3. Virtual pet + karma/dharma system
4. Reminders & calendar integration
5. No placeholders - everything works

Then later you can decide if you want the COVER FACE redesign based on user feedback.

**Run this:**
```powershell
cd C:\Users\Luke\Desktop\planner
Copy-Item life_fractal_v14_ultimate_complete.py -Destination app.py -Force
git add app.py
git commit -m "v14.0: Virtual Pet, Karma/Dharma, Calendar, Reminders - ALL FEATURES"
git push origin main
```

Wait 3 minutes, then test all the new endpoints! 🚀

═══════════════════════════════════════════════════════════════════════════════
