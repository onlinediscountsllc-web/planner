# Life Fractal Intelligence v8.0 - Dependencies
# =============================================

# Core Framework
Flask>=3.0.0
flask-cors>=4.0.0
Werkzeug>=3.0.0

# Data Processing
numpy>=2.0.0
Pillow>=10.0.0

# Optional ML (for mood prediction)
scikit-learn>=1.3.0

# Production Server
gunicorn>=21.0.0

# Note: The application uses SQLite which is built into Python
# No additional database dependencies required
