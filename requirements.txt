# Life Fractal Intelligence v10.0 - Requirements
# For Render.com deployment

# Core Flask
Flask==3.0.0
Flask-Cors==4.0.0
Werkzeug==3.0.1
gunicorn==21.2.0

# Data processing
numpy==1.26.2
Pillow==10.1.0

# ML (optional but recommended)
scikit-learn==1.3.2

# GPU Support (optional - will fall back to CPU if not available)
# torch  # Uncomment if GPU support needed
